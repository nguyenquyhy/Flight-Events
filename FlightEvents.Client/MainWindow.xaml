<Window x:Class="FlightEvents.Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FlightEvents.Client"
        mc:Ignorable="d" Loaded="Window_Loaded" StateChanged="Window_StateChanged"
        Title="Flight Events" Height="230" Width="300" ResizeMode="CanMinimize">
    
    <Grid Margin="10">
        <Grid.Resources>
            <Style TargetType="TextBlock">
                <Setter Property="Margin" Value="5" />
            </Style>
        </Grid.Resources>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="20" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <TextBlock Text="SimConnect" Grid.Row="0" Grid.Column="0" />
        <TextBlock Text="{Binding SimConnectionState}" Grid.Row="0" Grid.Column="1" />

        <TextBlock Text="Server" Grid.Row="1" Grid.Column="0" />
        <TextBlock Text="{Binding HubConnectionState}" Grid.Row="1" Grid.Column="1" />

        <TextBlock Text="Callsign" Grid.Row="2" Grid.Column="0" />
        <TextBox Text="{Binding Callsign, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Grid.Row="2" Grid.Column="1" MaxLength="10" />

        <TextBlock x:Name="TextURL" Text="https://localhost" 
                   TextDecorations="Underline" Foreground="Blue" Grid.ColumnSpan="2" Grid.Row="10" HorizontalAlignment="Center" VerticalAlignment="Bottom" 
                   MouseDown="TextURL_MouseDown"/>

        <Button x:Name="ButtonStartATC" Content="Start ATC Mode" Grid.Row="4" Grid.Column="1" Click="ButtonStartATC_Click" />
        <Button x:Name="ButtonStopATC" Content="Stop ATC Mode" Grid.Row="4" Grid.Column="1" Click="ButtonStopATC_Click" Visibility="Collapsed" />

        <tb:TaskbarIcon 
            x:Name="myNotifyIcon"
            xmlns:tb="http://www.hardcodet.net/taskbar"
            Visibility="Collapsed"
            ToolTipText="Honeycomb Connect"
            IconSource="/icon.ico"
            TrayLeftMouseDown="myNotifyIcon_TrayLeftMouseDown"
            />
    </Grid>
</Window>
